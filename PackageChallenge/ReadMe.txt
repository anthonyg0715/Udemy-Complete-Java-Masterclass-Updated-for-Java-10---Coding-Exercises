The PackageChallenge folder is a class with methods which is in a package and saved as a .jar file. The SeriesTest folder
is where the main class is and are able to call the methods from the PackageChallenge class because we imported the 
PackageChallenge class package into the main class.